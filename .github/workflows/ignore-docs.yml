name: Ignore Docs

on:
  push:
    branches:
      - main
      - 'release/*'

    paths-ignore:
      - "*.md"
      - "docs/**"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Code changes detected."
